# Contributing
## Commit format
Each commit should begin with the subproject it is targeting (or `ci`) followed by a colon and a commit message. For example:
```
frontend: Change default graph color to green
```