# Electrathon - `backend`
Python FastAPI project serving the backend for the Electrathon Dashboard.

## Building
### Dependencies
`uv` is used for package management. Running `uv sync` should be sufficient for installing dependencies into your virtual environment.
### Running
Run `fastapi dev` or `fastapi run`.